import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary: Vibrant Cyan/Turquoise from the new prototype
  static const Color primary = Color(0xFF26E1FF);

  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color background = Color(0xFFFFFFFF);

  // Additional harmonized colors
  static const Color border = Color(0xFFE5E5E5);
  static const Color cardBg = Color(0xFFF9F9F9);
  static const Color accentRed = Colors.redAccent;
  static const Color ratingStar = Color(0xFFFFB300);

  // Login Screen specific
  static const Color loginBackground = Color(0xFF121212);
  static const Color loginInputBg = Color(0xFFDDF4FF);
}
